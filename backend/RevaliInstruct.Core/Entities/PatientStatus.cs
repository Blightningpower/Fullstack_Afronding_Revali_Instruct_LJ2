namespace RevaliInstruct.Core.Entities
{
    public enum PatientStatus { IntakePlanned, Active, Completed, OnHold }
    public enum AppointmentStatus { Planned, Completed, Cancelled }
    public enum ClaimStatus { Pending, Declared }
}